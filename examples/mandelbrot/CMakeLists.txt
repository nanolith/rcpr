AUX_SOURCE_DIRECTORY(src MANDELBROT_SOURCES)

ADD_EXECUTABLE(mandelbrot ${MANDELBROT_SOURCES})
TARGET_LINK_LIBRARIES(mandelbrot PRIVATE rcpr)
TARGET_LINK_LIBRARIES(mandelbrot PRIVATE Threads::Threads)
